<!DOCTYPE html>
<html>
  <head>
    <%# <title>Banded | Join Forces, Make Music.</title> %>
    <meta name="Banded" content="Connect with passionate musicians, form bands, and create extraordinary music together. Join Banded now and unleash your musical potential.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  </head>

  <body>
    <div style="height: 66px">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>
    </div>

    <div class='content-wrapper'>
      <%= yield %>
    </div>

      <%= render "shared/footer" %>
  </body>
</html>
